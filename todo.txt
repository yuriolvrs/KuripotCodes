---------TODO---------
6/1/2026

[ ] - Figure out social media site scraping (big source) (facebook.ts stub removed; still needs a real implementation)
[ ] - Improve Filter UI


---------DONE---------
6/1/2026

[X] - Add manual "Working" checkbox
[X] - Set code as "used"
[X] - Add Filter for Working and Not Working

6/2/2026

[X] - Manual Add

6/3/2026

[X] - Other sources for Angkas (Everysaving working)
[X] - Other sources for other services (worthepenny working)
[X] - Add Bookmark feature and filter bookmarked
[X] - Overhaul UI
[X] - UI: fix platform icons and colors
[X] - Bug: View Bookmarked not working?
[X] - UI: sidebar and main window line alignment
[X] - UI/UX: Improve filter menu
[X] - UX: in Filter, when a Platform is selected, the services that show up in the dropdown should be of that platform only
[X] - Feat: add sort (and group by platform/service/status)

7/19/2026

[X] - Bigger lettering of platform and services
[X] - Better filtering from the start of codes that dont work anymore (added expired/stale pruning + garbage-code gate + one-time data cleanup)
[X] - Bug: Fix getting codes for other stores (traveloka, airpaz, emirates in Angkas Everysaving)
[X] - UI/UX: Fix button placement of filter, refresh, and Add
[X] - UX: make all worthepenny one source only when filtering
[X] - UI: checking Expiring, Bookmarked, and Used nudges the Filter sidebar to the right. it should stay in place
[X] - UI/UX: Promo code dropdown visibility bug, probably needs overhaul
[X] - UI: center code in container
[X] - UI/UX: long descriptions get cut off and there's no way to view the whole text (expand-in-place)
[X] - Removed dead/broken scrapers (couponfollow, facebook, official-pages, unused discovery plumbing)
[X] - Hardened scraper fetches with retries + throttled Reddit requests
[X] - Added Chrome/xvfb install to CI so WorthePenny can run headed there
[X] - Added toast notifications for copy/bookmark/working/used/scrape/add failures and successes
[X] - URL-persisted search/sort/group/quick-filters (survive reload, shareable links)
[X] - Keyboard shortcuts: "/" to focus search, Esc to clear it
[X] - Pending states on bookmark/working/used actions while saving
[X] - Used-card readability (muted background + strikethrough code instead of full opacity fade)
[X] - Dark mode with a sidebar toggle, persisted, and no flash-of-wrong-theme on load
[X] - Stable ids for codeless promos (title-based, not tied to sourceUrl) so bookmarks/flags survive a source URL changing
[X] - Added vitest + fixture/unit tests for scraper parsers and normalize/merge logic (38 tests)

7/19/2026 (scraping fix)

[X] - Bug: scraper wasn't finding new codes. Diagnosed: wethrift.com 403 masked by hardcoded fallback codes (removed, now surfaces as an honest failure); reddit www.reddit.com/*.json blocked (switched to old.reddit.com RSS + deduped the copy embedded in angkas.ts); rappler reveal-code fetches were silently rate-limited (throttled to concurrency 2, now succeeds ~50%+ of the time, logs N/M revealed)
[X] - New source: ivouchercodes.ph for Grab/Angkas/JoyRide (inline codes via data-clipboard-text, platform-filtered via the site's own stores-<slug> class)
[X] - Bug: iprice/rappler were tagging unrelated Lazada/foodpanda/etc ads as "Grab" promos because "grab" also reads as a verb in ad copy - added a foreign-brand blocklist veto (same pattern as the EverySaving fix)
[X] - Pipeline now reports per-scraper found counts + "new promos this run" in console and CI step summary, so a dead source is visible immediately instead of silently churning lastSeen timestamps
[X] - Verified via two consecutive local scrape runs: first run found 3 genuine new codes (HELLOGRABBER, GRABFREE, NOLATE) with zero junk; second run confirmed idempotence (0 new promos, no duplicate/drift)
